import React from 'react'
import ContentContainer from './Components/ContentContainer'

const NotFound = () => {
  return (
    <ContentContainer sx={{ display: 'flex', justifyContent: 'center' }}>
      <h2>Content not found</h2>
    </ContentContainer>
  )
}

export default NotFound
